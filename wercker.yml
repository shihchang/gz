box:
  id: golang
  ports:
    - "5000"

dev:
  steps:
    - internal/watch:
        code: |
          go build ./...
          ./source
        reload: true

# Build definition
build:
  # The steps that will be executed on build
  steps:

    # golint step!
    - wercker/golint

    # Build the project
    - script:
        name: go build
        code: |
          CGO_ENABLED=0 go build -a -ldflags '-s' -installsuffix cgo -o $WERCKER_OUTPUT_DIR/gz-hello . 

    # for dev only, create a local image with --docker-local option
    - internal/docker-scratch-push:
        repository: gz/hello
        tag: $WERCKER_GIT_BRANCH-$WERCKER_GIT_COMMIT
        cmd: ./gz-hello
        
push-wcr:
  box:
    id: alpine
    cmd: /bin/sh

  steps:
    - internal/docker-scratch-push:
        repository: wcr.io/wercker/hello
        tag: $WERCKER_GIT_BRANCH-$WERCKER_GIT_COMMIT
        registry: https://wcr.io/v2
        username: $WCR_USERNAME
        password: $WCR_PASSWORD
        cmd: ./gz-hello
        